class Solution:
    def twoSum(self, numbers: List[int], target: int) -> List[int]:
        i=0
        j=len(numbers)-1
        while j>i:
            a=numbers[i]+numbers[j]
            if a==target:return [i+1,1+j]
            elif a>target:j-=1   
            else:i+=1
